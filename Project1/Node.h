#pragma once
#include <vector>
using namespace std;
#include "WWW.h"

/// @brief  ADS_GraphNetwork master from lecture file included in project
struct Node {
public:
    virtual groupByRegionData& process(groupByRegionData& data);
};